# Urban-Resiliency-Geoportal

## Usage:
(All the commands have to be run in the root directory of the project)
### Download dependencies
```
python -m pip install -r requirements.txt
```

### Run the code
```
python manage.py runserver
```